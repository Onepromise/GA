function duel()
	var randomChoice = Math.random();
	var fiveChoice = Math.floor(randomChoice * 5);
	var choice = document.getElementByClass("button");

	for (i = 0; i <choice.length; i++) {
		currentChoice = choice[i];
	}
		if(currentChoice = function abilityChoice()){
			var selectedChoice = currentChoice.id;
		}
	}	
	var npcChoice = new Array("Paper", "Rock", "Scissors", "Lizard", "Spock")
	var reply = ""
	if (fiveChoice == 0){}
		if (selectedChoice == "Paper")
	}